export type Feature = {
  id: number;
  title: string;
  description: string;
  icon: string;
};

export type Testimonial = {
  id: number;
  name: string;
  role: string;
  content: string;
  avatar: string;
};

export type ProcessStep = {
  id: number;
  title: string;
  description: string;
  icon: string;
};